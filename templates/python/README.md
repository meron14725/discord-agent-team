# Agent project template

Python standard-library project for Discord Agent Team.

Run tests: `python -m unittest discover -s tests`

CI requires at least one discovered test and runs on Python 3.12 (Ubuntu 24.04).
Project changes belong in `src/` and `tests/`; the controller manages CI configuration.
